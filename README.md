# 矫务通 · 社区矫正管理系统

区司法局自研社区矫正管理系统「矫务通」。本仓库落地骨架与三个核心模块：

- **矫务作战台**：各司法所在矫漏斗、今日应报到、越界与违规红点
- **对象档案**：六态状态机、姓名脱敏与二次确认留痕、对象间数据隔离
- **定位监控**：腕表每 5 秒回传 GPS/设备状态，实时轨迹、多边形电子围栏（活动范围/禁区）、周月视图、越界落点二次核实、在矫完成度双口径
- **请销假**：对象手机端申请，司法所初审 → 区局复核两级审批，任一级退回可修改重提并逐轮留痕；假期内越界不报警（批假即消警），届满未销假自动升违规转训诫，支持手机销假
- **公益活动**：司法所发布活动点与打卡半径，对象手机端报名，现场打卡由服务端按半径重算距离，范围外打卡标异常且不可重复“洗白”
- **月度报到**：花名册勾选多个对象一次批量完成，逐条返回成功/跳过/失败与原因（越权、终态、已完成）

技术栈：Spring Boot 3.3（Java 17）+ Spring Data JPA + H2 + JWT；前端零依赖原生静态页面（自绘 SVG 地图）；Nginx 反代；Docker Compose 一键拉起。

## 一键启动

```bash
docker compose up -d --build
```

| 入口 | 地址 | 说明 |
|---|---|---|
| 前端（Nginx） | http://localhost:8104 | 管理端 + 对象手机端，同源反代 API |
| 后端直连 | http://localhost:7104 | Spring Boot，健康检查 `/api/health` |

前端容器会等后端健康检查通过后再启动。数据持久化在 Docker 卷 `jiaowutong-data`（H2 文件库），重启不丢数据；要恢复出厂种子数据：

```bash
docker compose down -v && docker compose up -d --build
```

## 演示账号（密码统一 `123456`）

| 账号 | 角色 | 数据范围 |
|---|---|---|
| `jiandu` | 监管员（区司法局） | 全区 4 个司法所，只读监管 |
| `gancheng` | 城关司法所干警 | 仅城关所对象，可处置状态 |
| `ganqingshan` | 青山司法所干警 | 仅青山所（乡村所）对象 |
| `ganlonghu` | 龙湖司法所干警 | 仅龙湖所对象 |
| `ganyining` | 伊宁司法所干警（**跨时区 UTC+6**） | 仅伊宁所对象 |
| `obj4` | 矫正对象·陈大山（青山所） | 仅本人；手机端，预置越界/漂移/低电场景 |
| `obj5` | 矫正对象·杨春生（青山所） | 仅本人；今日应报到 |

另有对象账号 `obj1/obj2/obj3/obj6/obj7/obj8/obj9/obj10`。

## 预置数据（开箱即真实数据）

- **4 个司法所**：城关、青山（丘陵山区，信号差）、龙湖，以及跨时区的**伊宁司法所（Asia/Urumqi，UTC+6）**
- 每所配 **多边形活动范围**（ALLOW_RANGE）与 **多边形禁区**（FORBIDDEN，含全天禁入 / 每日 20:00–次日 06:00 / 周五六 22:00–次日 05:00 跨午夜排程）
- **13 名矫正对象**，覆盖全部状态：入矫登记、在矫、请假外出、训诫、收监、解除
- **腕表 5 秒粒度轨迹**：在线行走、越界（陈大山）、闯入禁区（周文斌·废弃码头）、离线补传、未佩戴（张伟国）、低电/弱网、GPS 漂移跳点（被丢弃不连线）
- **两种空态样本**：孙满堂从无轨迹；徐建华轨迹已被清除（带清除留痕）
- **完成度双口径样本**：杨春生只踩报到节点（关键报到口径高、打卡天数口径低）；周文斌只在非报到日打卡（打卡天数 60%、关键报到 0%，两口径相反）
- **红点**：今日未报到（张伟国）、定位越界（陈大山）、禁区闯入（周文斌）、训诫记录
- **今日应报到**：种子按“对象所在司法所时区的今天星期几”动态生成
- 每个档案带合法路径的状态流转留痕

## 核心业务规则

### 1. 档案状态机（非法回退拦截 + 原因说明）

```
入矫登记 ──→ 在矫 ──→ 请假外出 ──→ 在矫（销假）
              │  └──→ 训诫 ──→ 在矫（教育恢复）
              │        └──→ 收监（终态）
              ├──→ 收监（终态）
              └──→ 解除（终态）
请假外出 ──→ 训诫（逾假不归）
```

- 终态（收监/解除）不可回退；入矫登记不可回流；不允许跨状态跳跃
- 非法操作返回 `409 INVALID_TRANSITION`，消息说明**原因和当前允许的下一状态**，前端弹窗原样展示
- 每次流转写 `status_transition` 留痕（操作人、理由、时间），训诫同时生成违规红点

### 2. 姓名脱敏与二次确认留痕

- 列表/接口默认只返回脱敏名：**姓氏拼音首字母-矫正编号**（如 张伟国 → `Z-JWT26001`），`fullName` 字段不下发
- 查看全名必须二次确认弹窗 + **填写不少于 4 字的工作理由**
- 每次查看落 `name_view_audit`（查看人、理由、时间），档案页可回看全部留痕
- 矫正对象本人查看自己档案显示真名；看不到任何他人数据

### 3. 对象隔离 / 越权错误态

- 矫正对象：仅能访问本人档案；干警：仅本能访问本所对象；监管员：全区
- 越权访问（如对象 A 改 URL 访问对象 B）返回 **403 错误页**（说明原因），不是空白页或列表页
- 后端在 Service 层强制数据范围，不依赖前端隐藏

### 4. 乡村断网：离线定位、防旧位置、合并幂等

对象手机端（`obj4` 登录后进入“我的矫正”）：

- **离线提示**：断网时黄色横幅提示“本地缓存中（N 点）”，恢复后绿色提示并自动补传；可点「模拟断网」在无真机情况下演示
- **不能用旧位置糊弄**：每个定位点带 GPS 采集时间（`pointTime`），服务端按时钟强校验
  - 实时点采集时间超过 **5 分钟** → `422 STALE_LOCATION` 拒绝（报到和轨迹都一样）
  - 离线补传点超过 **72 小时**窗口 → 拒绝；未来时间 → 拒绝
  - 页面上「🧪 尝试用 2 小时前缓存旧定位报到」可直接验证
- **恢复后合并**：批量点按**采集时间**合并入轨迹，对象最新位置取采集时间最大者，乱序/迟到的历史点不会把当前位置冲回旧位置
- **幂等不重复**：手机为每个点生成 `clientPointId`（UUID），库内唯一约束；断网期间重复重放只计入 `duplicates`，**不产生重复轨迹点**
- 越界按司法所电子围栏判定（多边形活动范围，未配置多边形时回退中心圆）；仅在未处置越界红点不存在时报警一次，批量补传历史越界点不刷屏

### 5. 定位监控（新增）

左侧「🛰️ 定位监控」菜单进入。腕表**每 5 秒回传一帧** GPS + 设备状态（电量/信号/是否佩戴），管理端总览与轨迹均按 5 秒实时增量刷新。

**时间与时区（跨时区不算错）**

- 所有定位/报到/事件时间一律按 **UTC 存储与传输**（`Instant`，ISO 带 `Z`），JVM 与 Jackson 默认时区固定 UTC，不随容器 `TZ` 漂移
- 司法所带 `timezone`（IANA，如 `Asia/Shanghai`、`Asia/Urumqi`）；“今天/星期几/是否禁行/是否同一天”全部先把 UTC 转到**对象所属司法所时区**再判，前端也按该时区显示——干警跨时区查看不会替对象换时区
- 禁行时段用地区名时区（非固定偏移），**夏令时切换由 tzdb 规则处理**：纽约测试用例覆盖春进（不存在的 02:30）与回拨（重复的 01:00–02:00），切换日不误报、不漏报；跨午夜时段（22:00–05:00）凌晨的点回溯“前一天起始星期”判同一次禁行

**GPS 漂移过滤（不只比距离）**

- 点按采集时间排序后逐点比对上一**可信锚点**：等效速度 > 45 m/s（约 162 km/h）且跳变 ≥ 100 m 判为漂移
- 漂移点标 `DRIFT_DISCARDED` **留痕但不连线、不移动锚点、不更新位置、不触发越界**；下一点仍以原可信点比对，避免被跳点带偏（小于 100 m 的常规抖动吸收）

**多边形围栏与禁区**

- 活动范围 ALLOW_RANGE（ray-casting 点面判定，支持凹多边形；未配多边形回退中心圆）；禁区 FORBIDDEN 多边形 + 可选按星期/跨午夜时段排程
- 几何命中且处于禁行时段才算“禁区闯入”；无排程的禁区全天禁行

**周/月两视图与落点核实**

- 周视图 7 天 / 月视图 30 天，时间窗按对象时区的当前时刻回取再转 UTC 查询；首屏抽稀（越界/禁区/离线点全保留），`since` 增量轮询不重传全量
- 地图为零依赖自绘 SVG（等距经纬度投影、围栏多边形、轨迹线、越界红点/禁区紫点、当前位置脉冲）
- 对“会越界”的落点可进入核实模式，**二次确认 + 必填原因留痕**；服务端用当前围栏几何与该所时区**重新计算**该点，把正常点误标“确认越界”会被拒绝（`POINT_NOT_ANOMALY`）

**三种空态/错误态分开（不统一“暂无数据”）**

1. 加载失败：错误态 + 原因 + 「重新加载」，说明设备仍在回传
2. 该对象无轨迹：提示腕表未激活/无有效帧（漂移点不计入轨迹）
3. 轨迹已清除：显示清除时间（对象时区）、操作人、点数、原因的留痕卡片

清除轨迹是高危操作：二次确认 + 必填原因，删除并清空实时位置，落 `monitor_action` 留痕。

**在矫完成度（口径写在界面上）**

- 近 30 天双口径并列，定义直接上墙：**口径一·打卡天数** = 实际有报到天数 ÷ 30（非报到日额外打卡也计入）；**口径二·关键报到** = 规定报到日当天完成次数 ÷ 应到节点数（只认节点）
- 只部分报到的对象两口径可能相反（种子周文斌 60% vs 0%），此时界面显著告警，提示不能只报一个百分数

### 6. 作战台

- 顶部 4 个指标：监外执行中、建档总数、今日应报到、未处置红点
- 全区漏斗：入矫登记/在矫/请假外出/训诫/收监/解除（在矫口径 = 在矫+请假+训诫）
- 各司法所分段漏斗条（悬停看各状态人数）+ 完整图例（颜色不单独承载语义）
- 今日应报到：已报到 / 待报到 / 逾时未报（按对象所在司法所时区的当地 18:00 判定）
- 红点：越界 / 未报到 / 训诫，点击直达档案
- 响应式适配三档：**1440 桌面**（侧栏+双列）、**1024 平板**（图标侧栏）、**390 手机**（底部导航、表格转卡片）

### 7. 请销假（两级审批 + 定位联动）

对象手机端「📝 请销假」发起，干警/区局在「请销假审批」工作台办理：

```
对象申请 → 司法所初审 → 区局复核 → 批准（档案自动转「请假外出」）→ 对象手机销假（回「在矫」）
                └ 任一级退回（必须填≥4字意见）→ 对象修改后同一单据重提（revision+1，两级重走）
批准假期届满仍未销假 → 定时任务（每 60 秒）自动转「训诫」+ 生成「逾假未归」违规红点（OVERDUE，幂等）
```

- **逐轮留痕**：提交 / 初审通过·退回 / 复核通过·退回 / 重提 / 销假 / 逾期自动处置全部追加到 `leave_request_log`，每轮申请内容（目的地/事由/起止）做快照，退回重提不覆盖历史
- **不能越级**：区局账号不能做司法所初审，司法所账号不能做区局复核；不在本所的单子干警不能办
- **定位联动（不能一边批着假一边报着警）**：
  - 区局复核通过的一刻，先把该对象所有未处置的越界/禁区红点**核销**；
  - 已批准假期窗口 `[startAt,endAt)` 内，轨迹点照常入库留痕，但**越过活动围栏/进入禁区不产生红点**；作战台「今日应报到」也自动豁免假期内对象；
  - 假期届满未销假被自动转训诫后，APPROVED 单据消失，越界报警**自动恢复**（已用 HTTP 用例验证销假前后报警的关/开）；
  - 通用状态流转接口**禁止手工直接切到「请假外出」**（`LEAVE_FLOW_REQUIRED`），必须走两级审批，避免绕过消警/逾期联动
- 请假须事先申请（开始时间不得早于当前 1 小时），单次最长 90 天；审批期间档案若已变为非「在矫」，区局准假会被拦下

### 8. 公益活动（手机报名 + 现场打卡位置校验）

司法所/区局在「🤝 公益活动」发布（活动点坐标 + 打卡半径 20~5000 米 + 起止/报名截止/容量）；对象在手机端看到**本所**活动并报名：

- **报名**：仅本所对象、非收监/解除状态；报名截止后与活动开始后不可报；名额满拒绝；**幂等**（重复报名不报错不重复计数）
- **现场打卡**：必须先报名、在活动开始前 30 分钟~结束窗口内、定位采集时间 ≤5 分钟（旧位置一律拒绝，与报到同口径）
- **位置由服务端重算**：Haversine 距离 ≤ 活动点半径记 `NORMAL`；**超出半径记 `ABNORMAL` 位置异常**，记录实际距离与坐标，留痕但不采信为到场
- **异常不可洗白**：重复打卡返回首次记录（`alreadyChecked=true`），异常后跑到现场再打也无法覆盖；管理端花名册逐人显示正常/未打卡/异常及距离
- 对象只能看本所活动，干警只能看/发本所活动，跨所报名、跨所办理均 403

### 9. 月度报到（批量完成 + 逐条结果）

「🗓️ 月度报到」按月列花名册（月份按对象所属司法所时区解释），勾选多个对象一次批量完成：

- 服务端**逐人独立校验、独立事务落库**，任一对象失败不回滚其他人；
- 返回每个人的 `SUCCESS / SKIPPED / FAILED` 与逐条说明：本月已完成→`SKIPPED`（防重复，唯一约束兜底）；档案不存在、跨所越权、收监/解除终态、未来月份→`FAILED` 并写明原因；
- 结果弹窗按成功/跳过/失败分组，逐条说清「谁成了、谁卡在哪」

## 目录结构

```
├── docker-compose.yml          # backend(7104) + frontend(8104)
├── backend/
│   ├── Dockerfile              # 多阶段：Maven 构建 → JRE 运行
│   ├── pom.xml
│   └── src/main/java/cn/sfj/jiaowutong/
│       ├── domain/             # 实体与状态枚举
│       ├── repo/               # JPA 仓储
│       ├── security/           # JWT、PBKDF2、鉴权拦截器
│       ├── service/            # 状态机/脱敏/作战台/轨迹幂等/围栏几何/禁行排程/定位监控/访问控制
│       ├── web/                # 控制器 + dto + vo
│       └── config/DataInitializer.java  # 种子数据（4 所/多边形围栏/5 秒轨迹/双口径样本）
└── frontend/
    ├── Dockerfile              # nginx:alpine + 静态资源
    ├── nginx.conf              # SPA 回退 + /api 反代 backend:7104
    └── html/
        ├── index.html
        └── assets/             # 零依赖原生 JS/CSS
            ├── api.js  ui.js  offline-queue.js  app.js
            └── views/          # login/dashboard/objects/detail/monitor/offender
```

## 本地开发（不用 Docker）

```bash
cd backend
mvn spring-boot:run            # 需要本机 JDK 17+；默认 H2 内存库
# 前端静态托管任选其一：
cd frontend/html && python3 -m http.server 8104   # 此时 API 直连 7104（已开 CORS），需把 api.js 前缀改为绝对地址
```

## 主要 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 登录换 JWT |
| GET | `/api/dashboard` | 作战台聚合（按角色裁剪数据范围） |
| GET | `/api/objects?status=&officeId=` | 档案列表（脱敏） |
| GET | `/api/objects/{id}` | 档案详情（越权 403 错误态） |
| POST | `/api/objects/{id}/transition` | 状态流转（非法回退 409 + 原因） |
| POST | `/api/objects/{id}/reveal-name` | 二次确认 + 理由 → 返回全名并留痕 |
| GET | `/api/objects/{id}/name-audits` | 全名查看留痕 |
| GET | `/api/objects/{id}/tracks` | 有效轨迹点 |
| GET | `/api/monitor/overview` | 定位监控总览（实时位置/心跳/设备状态，5s 轮询） |
| GET | `/api/monitor/objects/{id}/tracks?range=WEEK\|MONTH&since=` | 周/月轨迹（首屏抽稀，since 增量） |
| POST | `/api/monitor/objects/{id}/points/{pointId}/verify` | 越界落点二次核实（服务端重算 + 原因留痕） |
| POST | `/api/monitor/objects/{id}/clear-tracks` | 清除轨迹（必填原因留痕） |
| GET | `/api/monitor/objects/{id}/actions` | 清除/核实操作留痕 |
| POST | `/api/offender/tracks` | 腕表轨迹批量上报（UTC、幂等/合并/时效/漂移过滤/禁区判定；批准假期内抑制越界红点） |
| POST | `/api/offender/check-in` | 日常报到（UTC 定位时效校验，按所时区记日报到） |
| POST | `/api/offender/leaves` | 对象提交请假申请 |
| GET | `/api/offender/leaves` / `/{id}` | 我的请假单 / 单据详情（逐轮审批留痕） |
| POST | `/api/offender/leaves/{id}/resubmit` | 退回后修改重提（revision+1，两级重走） |
| POST | `/api/offender/leaves/{id}/return` | 对象手机销假返所 |
| GET | `/api/leaves?stage=OFFICE\|BUREAU\|RETURNED\|ACTIVE\|ALL` | 请销假审批工作台（按角色/数据范围过滤） |
| POST | `/api/leaves/{id}/office-review` | 司法所初审（approve=true 通过 / false 退回，退回必填意见） |
| POST | `/api/leaves/{id}/bureau-review` | 区局复核（通过即准假并联动消警，退回重提） |
| POST | `/api/leaves/{id}/return` | 干警代对象销假 |
| GET/POST | `/api/activities` | 公益活动列表 / 发布（活动点坐标+打卡半径） |
| GET | `/api/activities/{id}` | 活动详情 + 报名/打卡花名册（异常打卡逐条可见） |
| GET/POST | `/api/offender/activities` | 对象看本所活动 / `/{id}/signup` 手机报名（幂等） |
| POST | `/api/offender/activities/{id}/check-in` | 现场打卡（服务端半径校验，范围外标 ABNORMAL 不可洗白） |
| GET | `/api/monthly-reports?month=yyyy-MM` | 月度报到花名册（含完成状态） |
| POST | `/api/monthly-reports/batch-complete` | 批量完成，逐条返回 SUCCESS/SKIPPED/FAILED 与原因 |
